# Vision + inference
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0

# Screen capture and synthetic input on macOS. The mirror window is read with
# Quartz and driven with CGEvent, so these are not optional extras.
pyobjc-framework-Quartz>=10.0
pyobjc-framework-Cocoa>=10.0

# Tower HP digits. Needs the tesseract binary too, which install.sh handles.
pytesseract>=0.3.10

# OpenAI Responses API calls
requests>=2.31.0
